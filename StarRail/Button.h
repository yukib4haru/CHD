#ifndef BUTTON_H
#define BUTTON_H

#include<star.h>

class Button : public QPushButton
{
    Q_OBJECT
public:
    Button();
    ~Button();
    Button(QWidget* p);
public slots:
};

#endif // BUTTON_H
